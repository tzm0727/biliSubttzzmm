# biliSub 发布部署指南

本文档说明如何把 biliSub 推送到 GitHub，以及如何在 Render 上完成线上部署与更新。

## 1. 项目基本信息

- GitHub 仓库地址：<https://github.com/tzm0727/biliSubttzzmm>
- Git 克隆地址：<https://github.com/tzm0727/biliSubttzzmm.git>
- 线上网址：<https://bilisub.onrender.com>
- 当前版本：Web v6

## 2. 如何推送到 GitHub

### 方式一：命令行推送（推荐）

本地已有一份 Git 仓库：

```text
C:\Users\Administrator\Documents\Codex\2026-08-04\ni\work\biliSubttzzmm
```

在电脑上打开 PowerShell，进入该目录：

```powershell
cd C:\Users\Administrator\Documents\Codex\2026-08-04\ni\work\biliSubttzzmm
```

修改完代码后，依次执行三条命令：

```powershell
git add -A
git commit -m "更新说明"
git push origin main
```

说明：

- 每次修改后重复这三条命令即可更新 GitHub。
- 推送到 `main` 分支后，Render 会自动重新部署（约 1~3 分钟）。
- 如果提示需要身份信息，先执行：

```powershell
git config user.name "你的名字"
git config user.email "你的邮箱"
```

### 方式二：GitHub 网页上传（不装 Git 也能用）

1. 打开 <https://github.com/tzm0727/biliSubttzzmm>
2. 点击 **Add file → Upload files**
3. 把要更新的文件拖进去（保持原有目录结构：`public`、`server.js`、`package.json`、`render.yaml` 等）
4. 写一句提交说明，点击 **Commit changes**

> 注意：网页上传容易漏文件或破坏目录结构，推荐使用方式一。

## 3. Render 线上部署流程

### 首次部署

1. 打开 <https://render.com>，用 GitHub 账号登录
2. 点击 **New + → Blueprint**（或 Web Service），选择仓库 `tzm0727/biliSubttzzmm`
3. 选择仓库后，Render 会自动识别根目录的 `render.yaml`
4. 填写两个环境变量：
   - `DEEPSEEK_API_KEY`：你的 DeepSeek API Key（议题成文、字幕润色、生成文章都会用到）
   - `ACCESS_TOKEN`：自己设置一个访问口令，手机打开网站时输入
5. 点击 **Apply / Deploy**，等待 3~5 分钟构建完成
6. 完成后打开 <https://bilisub.onrender.com>

### 自动部署（每次更新代码后）

- 每次 `git push` 到 `main`，Render 会自动拉取最新代码并重新部署
- 部署过程中旧版本仍然可以访问，新版本就绪后自动切换

### 手动部署（自动部署失败或不生效时）

1. 打开 Render 控制台：<https://dashboard.render.com>
2. 进入服务 `bilisub`（Service ID：`srv-d9qa5sajobas73bngjjg`）
3. 点击右上角 **Manual Deploy → Deploy latest commit**
4. 等待构建日志依次显示：`Build successful` → `Deploying` → `Running 'node server.js'`

### 查看部署日志 / 排查失败

- 在服务页面点击 **Events** 或 **Logs** 查看构建和运行日志
- 常见失败原因：
  - `npm install` 失败：检查网络或依赖是否完整
  - 启动报错（如 `ReferenceError`、语法错误）：先在本地执行 `node --check server.js` 检查语法
  - 部署成功但打开很慢：可能是免费实例休眠，等 30~60 秒再刷新

## 4. 免费实例注意事项

- Render 免费实例闲置 15 分钟会休眠，再次访问需要 30~60 秒唤醒
- 手机第一次打不开时，等 30~60 秒再刷新一次即可
- 如果希望 24 小时不休眠，可以用 UptimeRobot 等免费监控服务每 5 分钟自动访问一次
- 升级付费实例（约 7 美元/月）可以永不休眠

## 5. 项目结构速览

- `server.js`：Node 服务端（B 站字幕接口 + 议题成文 + DeepSeek 代理）
- `public/`：手机网页前端（下载、生成文章、文档、阅读器、设置）
- `render.yaml`：Render 部署配置
- `package.json`：依赖与启动命令
- `README.md` / `云部署说明.md`：功能与云部署说明
