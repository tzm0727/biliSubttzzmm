# biliSub Web 项目代码理解文档

> 适用版本：Web v7（模块化重构版，2026-08 重构）
> 本文档基于对 `biliSub-cloud-package`（开发工作目录）与 `biliSubttzzmm`（Git 仓库）两份源码的完整通读编写。
> v7 起：后端 `server.js` 拆分为 `lib/` 下 9 个 CommonJS 模块，前端 `app.js` 拆分为 8 个视图模块（`window.ui` 命名空间），新增 `sync-to-git.ps1` 同步脚本。**功能与 v6 完全一致，纯结构重构。**
> 两份源码经 SHA-256 校验**完全一致**，唯一差异是工作目录多出 `node_modules`、`.npm-cache`、`server-config.json` 等本地运行时文件（均被 `.gitignore` 排除）。

---

## 目录

1. [项目定位与功能总览](#1-项目定位与功能总览)
2. [两个文件夹的关系与工作流](#2-两个文件夹的关系与工作流)
3. [目录结构与文件清单](#3-目录结构与文件清单)
4. [技术栈与依赖分析](#4-技术栈与依赖分析)
5. [后端 server.js 详解](#5-后端-serverjs-详解)
6. [前端架构与模块详解](#6-前端架构与模块详解)
7. [核心工作流拆解](#7-核心工作流拆解)
8. [数据存储方案](#8-数据存储方案)
9. [安全与配置机制](#9-安全与配置机制)
10. [部署与发布流程](#10-部署与发布流程)
11. [关键实现细节与注意事项](#11-关键实现细节与注意事项)
12. [常见问题排查](#12-常见问题排查)

---

## 1. 项目定位与功能总览

**biliSub Web** 是一个"B 站字幕下载 + AI 议题成文"的网页应用，设计为手机浏览器直接使用（也可在电脑浏览器使用），未来可打包为安卓 App。

核心功能五大块：

| 功能 | 说明 | 关键代码 |
|---|---|---|
| **扫码登录 B 站** | B 站 App 扫码登录，Cookie 仅存本机浏览器 | `server.js` 登录代理 + `app.js` 二维码轮询 |
| **字幕下载** | 粘贴 B 站链接（BV/av/b23.tv/分P），下载字幕，支持 TXT/SRT/ASS/VTT/JSON/LRC 六种格式；自动选择语言（中文 > 英文 > 其他） | `app.js` `startDownload()` |
| **议题成文** | 输入议题 → AI 规划大纲 → 免费搜索资料 → 分章写作 → 导语 → 审校修订 → 生成 3000/6000/10000 字长文（带编号引用与参考资料） | `server.js` 议题成文引擎（SSE 流式进度） |
| **文件管理** | 导入、搜索、阅读、重命名、删除、导出（含 Web Share API） | `app.js` + `storage.js` |
| **AI 润色/科普** | 字幕润色（transcript-polisher 技能）、生成大众科普文章（science-writing 技能）、AI 精简文件名，均调用 DeepSeek | `ai.js` + `public/skills/` 提示词 |

设计理念要点：

- **零框架、零构建**：后端用 Node 原生 `http` 模块，前端用原生 JS（IIFE 全局对象模式），无打包步骤，`node server.js` 直接跑。
- **数据全在浏览器**：文件存 IndexedDB，设置/Cookie/阅读位置存 localStorage，不建服务端数据库。
- **服务端只做代理与编排**：B 站接口、DeepSeek 接口都由 `server.js` 代理转发（解决浏览器跨域与 Cookie 问题），AI 长文生成由服务端编排。
- **手机优先**：界面为移动端单页应用，支持 Web Share（导出分享）、夜间模式、阅读位置记忆。

---

## 2. 两个文件夹的关系与工作流

```
D:\deepseek_h_pro\
├── biliSub-cloud-package\   ← 开发工作目录（最完整，改代码在这里）
│   ├── node_modules\        ← 依赖（38MB，不进 Git）
│   ├── .npm-cache\          ← npm 本地缓存（不进 Git）
│   ├── server-config.json   ← 本地密钥配置（可选，.gitignore 排除）
│   └── ...源码文件（22 个）
└── biliSubttzzmm\           ← Git 仓库（推送到 GitHub 用）
    ├── .git\                ← 仓库元数据
    └── ...源码文件（22 个，与上面完全一致，无 node_modules）
```

### 开发工作流（v7 起提供一键脚本）

```
1. 修改代码         → 在 biliSub-cloud-package 中编辑
2. 本地测试         → 在 biliSub-cloud-package 运行 node server.js，浏览器打开 http://127.0.0.1:8324
3. 同步 + 提交 + 推送 → 运行 sync-to-git.ps1，自动复制源码到 biliSubttzzmm 并 git 提交/推送
4. Render 自动部署  → 线上 https://bilisub.onrender.com 自动更新（1~3 分钟）
```

**sync-to-git.ps1 用法**（放在 biliSub-cloud-package 根目录）：

```powershell
.\sync-to-git.ps1                              # 仅同步文件，不提交
.\sync-to-git.ps1 -Message "更新说明"           # 同步 + git commit
.\sync-to-git.ps1 -Message "更新说明" -Push     # 同步 + commit + push（触发 Render 部署）
.\sync-to-git.ps1 -NoCommit                    # 明确只同步、不提交
```

脚本自动排除 `node_modules`、`.npm-cache`、`.git`、`server-config.json`、日志与隧道工具（与 `.gitignore` 一致），目标仓库路径在脚本顶部 `$Dst` 变量中配置。

> 也可以继续手动：复制源码到 biliSubttzzmm（保持目录结构）→ `git add -A && git commit && git push`。复制时**不要**带上 `node_modules`、`.npm-cache`、`server-config.json`。

### 线上部署链路

```
GitHub (tzm0727/biliSubttzzmm)  ──push──▶  Render (bilisub.onrender.com)
                                              ├─ buildCommand: npm install --omit=dev
                                              └─ startCommand: node server.js
```

---

## 3. 目录结构与文件清单

```
biliSub-cloud-package/
├── server.js                  # 入口：HTTP 服务、静态文件、请求分发（精简，~100 行）
├── lib/                       # 后端模块（CommonJS，v7 从 server.js 拆分）
│   ├── constants.js           # 运行时常量（端口/目录/UA/MIME/WBI 表/搜索源）
│   ├── config.js              # 配置加载（环境变量 + server-config.json）
│   ├── utils.js               # 通用工具（HTML 解码/文本提取/网络抓取/请求体/响应/链接解析）
│   ├── cookie-jar.js          # B 站 Cookie 罐（内存）
│   ├── bili-client.js         # B 站 HTTP 客户端 + WBI 签名
│   ├── deepseek.js            # DeepSeek 对话封装
│   ├── search.js              # 免费搜索源聚合（维基/DuckDuckGo/Bing/SearXNG）
│   ├── article-engine.js      # 议题成文引擎（规划/搜索/写作/导语/审校/修订）
│   └── routes.js              # API 路由
├── sync-to-git.ps1            # 开发工作流脚本（同步源码到 Git 仓库并提交/推送）
├── package.json               # 项目元信息、依赖、启动脚本
├── package-lock.json          # 依赖锁定
├── render.yaml                # Render 云部署配置
├── README.md                  # 项目简介与使用说明
├── 云部署说明.md               # 面向用户的免费云部署教程
├── 发布部署指南.md             # 面向作者的推送/部署指南（含仓库地址、Service ID）
├── .gitignore                 # 排除 node_modules / server-config.json / 日志 / bat 等
└── public/                    # 前端静态资源（手机网页）
    ├── index.html             # 单页入口（5 视图 + 2 弹窗）
    ├── css/style.css          # 全部样式（设计系统：配色 token + 深浅主题 + 微交互）
    ├── js/                    # 前端模块（v7 从 app.js 拆分，零构建 IIFE + window.ui 命名空间）
    │   ├── storage.js         # 数据层：IndexedDB 文件库 + localStorage 设置
    │   ├── api.js             # 通信层：后端 API 封装（含 SSE 解析）
    │   ├── ai.js              # AI 功能层：润色/科普/精简文件名
    │   ├── core.js            # 核心：state + 工具函数 + 视图切换 + 主题/字号 + AI 设置
    │   ├── login.js           # 扫码登录（二维码生成/轮询 + 手动 Cookie）
    │   ├── download.js        # 字幕下载（链接解析/清洗/格式转换）
    │   ├── docs.js            # 文档管理（列表/搜索/导入/导出/重命名/删除）
    │   ├── reader.js          # 阅读器（打开/Markdown 渲染/位置记忆）
    │   ├── ai-modal.js        # AI 弹窗（润色/科普进度与调用）
    │   ├── article.js         # 议题成文（SSE 事件处理/进度/保存）
    │   └── app.js             # 入口（初始化 + 事件绑定）
    ├── libs/
    │   ├── marked.min.js      # Markdown 渲染库（阅读器用，浏览器版）
    │   └── qrcode.min.js      # 二维码生成库（扫码登录用，浏览器版）
    └── skills/                # AI 提示词（技能文件，前端 fetch 后拼进 system prompt）
        ├── transcript-polisher/          # 字幕润色技能（MIT 开源，rookie-ricardo/erduo-skills）
        │   ├── SKILL.md                  # 主技能（340 行）
        │   └── references/
        │       ├── common-errors.md      # 高频错误参考（拼进润色 system prompt）
        │       └── examples.md           # 示例（当前代码未引用，供参考）
        └── science-writing/              # 科普文章技能（MIT 开源，Ariclk-L/science-writing-skill）
            ├── SKILL.md                  # 主技能（286 行）
            └── references/
                ├── rhetoric-discipline.md   # 修辞纪律 8 条（拼进科普 system prompt）
                └── writing-guidelines.md    # 写作规范（拼进科普 system prompt）
```

---

## 4. 技术栈与依赖分析

### 4.1 运行时零第三方依赖（关键事实）

`server.js` 只 require 了 Node **内置模块**：

```js
const http = require("http");
const fs = require("fs");
const path = require("path");
const crypto = require("crypto");
```

其余能力全部来自 Node 18+ 全局 API：`fetch`（B 站/DeepSeek/搜索请求）、`AbortController`（取消/超时）、`TextDecoder`、`URL`、`Promise.allSettled` 等。因此 **`npm install` 理论上并非运行必需**，只有 `node server.js` 足够。

### 4.2 声明的依赖（package.json）

| 包 | 版本 | 类型 | 实际用途 |
|---|---|---|---|
| `marked` | ^18.0.9 | dependencies | 仅前端阅读器渲染 Markdown（`public/libs/marked.min.js` 已是浏览器版拷贝） |
| `qrcode` | ^1.5.4 | dependencies | 仅前端二维码渲染（`public/libs/qrcode.min.js` 已拷贝） |
| `esbuild` | ^0.28.1 | devDependencies | 疑似用于压缩/拷贝 libs 的构建工具，当前源码无引用 |

> 说明：`public/libs` 里的两个 min.js 是 npm 包的浏览器版成品文件，直接随源码分发，浏览器 `<script>` 加载；服务端代码并未 `require` 它们。

### 4.3 node_modules 中的残留包（重要）

`node_modules` 里存在一批**不在 package.json / package-lock.json 中**的包：

- `youtube-transcript-plus@2.0.1`
- `get-youtube-transcript@1.0.0`
- `bgutils-js@3.2.0`
- `jsdom@24.1.3`（及 `ws`、`tough-cookie`、`html-encoding-sniffer` 等 jsdom 依赖树）

**结论**：这些是历史版本（可能曾支持 YouTube 字幕下载）安装后遗留的，当前代码完全不使用。Git 仓库和 Render 部署时执行 `npm install --omit=dev` 只会安装 lock 文件里的包（marked/qrcode/esbuild 系），**不会**安装这些残留包，线上行为不受影响。如需清理本地，可删掉后重新 `npm install`。

### 4.4 前端技术

- 原生 JS（ES2017+），无框架、无模块打包，每个 JS 文件是 IIFE。
- 命名空间：`window.storage`（数据层）/ `window.api`（通信层）/ `window.ai`（AI 功能层）保持独立；其余视图模块统一挂在 `window.ui` 命名空间下（v7 起），通过 `ui.state`、`ui.$`、`ui.switchView` 等互相调用。
- 引入顺序（index.html 末尾，v7）：`qrcode.min.js` → `marked.min.js` → `storage.js` → `api.js` → `ai.js` → `core.js` → `login.js` → `download.js` → `docs.js` → `reader.js` → `ai-modal.js` → `article.js` → `app.js`。**core.js 必须先于其它视图模块**（它定义 `window.ui` 命名空间），`app.js` 最后（绑定 DOMContentLoaded）。
- 样式用 CSS 变量实现浅色/深色主题切换（`body.dark` 覆盖 `:root` 变量）。

---

## 5. 后端模块详解（v7 起拆分）

v7 起，原 1472 行的 `server.js` 拆分为「入口 + `lib/` 下 9 个 CommonJS 模块」，逻辑不变：

| 模块 | 职责 | 依赖 |
|---|---|---|
| `server.js` | 入口：HTTP 服务、静态文件、口令校验、请求分发 | constants/config/routes/utils |
| `lib/constants.js` | 运行时常量（PORT/PUBLIC_DIR/UA/MIME/WBI 表/搜索源） | — |
| `lib/config.js` | 配置加载（环境变量优先 + server-config.json） | constants |
| `lib/utils.js` | 通用工具（HTML 解码/文本提取/网络抓取/请求体/响应/链接解析） | constants |
| `lib/cookie-jar.js` | B 站 Cookie 罐（内存 jar） | — |
| `lib/bili-client.js` | biliGet / followWithCookies / WBI 签名 | cookie-jar/constants |
| `lib/deepseek.js` | deepseekChat 封装 | — |
| `lib/search.js` | 免费搜索源聚合 + 缓存 | constants/utils |
| `lib/article-engine.js` | 议题成文引擎（规划/搜索/写作/导语/审校/修订） | deepseek/search/utils |
| `lib/routes.js` | API 路由（含 activeAIRequests 取消表） | 以上各模块 |

### 5.1 启动与配置加载

```js
// lib/constants.js
const PORT = Number(process.env.PORT || 8324);        // Render 会注入 PORT
const BIND_HOST = process.env.BIND_HOST || "127.0.0.1"; // 本地默认仅本机；Render 注入 0.0.0.0
```

`lib/config.js` 的 `loadServerConfig()` 合并两处配置（**环境变量优先**）：

1. 环境变量：`DEEPSEEK_API_KEY`、`ACCESS_TOKEN`
2. 本地文件 `server-config.json`（可选，被 .gitignore 排除，不会上传 GitHub）

### 5.2 全局状态（内存）

| 状态 | 类型 | 用途 |
|---|---|---|
| `jar` | `Map` | B 站 Cookie 罐，键为 `domain|path|name`，登录/请求时由前端同步 |
| `activeAIRequests` | `Map` | 进行中的 AI 请求 `requestId → AbortController`，用于取消 |
| `wbiCache` | `{keys, at}` | WBI 签名密钥缓存（12 小时过期） |
| `articleSearchCache` | `Map` | 议题搜索缓存（query → 结果，30 分钟过期，上限 300 条） |

### 5.3 B 站 Cookie 罐（`jar`）

设计：B 站登录 Cookie **不落服务端磁盘**，只存在内存 `jar`；浏览器把 Cookie 存在自己的 localStorage，每次页面加载通过 `POST /api/session/cookies` 全量同步给服务端。

- `parseSetCookie(str)`：解析 Set-Cookie 头（name/value/domain/path/expires/secure）。
- `mergeSetCookies(url, list)`：把 B 站响应里的 Set-Cookie 合并进 jar；已过期（expires <= now）的删除。
- `buildCookieHeader(url)`：按 域名匹配 + 路径前缀 + secure 协议 规则，拼出要发给 B 站的 Cookie 头。
- `cookiesArray()` / `setCookiesArray()`：与前端之间序列化/反序列化。

### 5.4 B 站请求封装

- `biliGet(url, referer)`：带 UA/Referer/Origin/Accept-Language 头请求 B 站，`redirect: "manual"` 手动处理重定向，自动合并 Set-Cookie，返回 `{status, headers, json, text}`。
- `followWithCookies(url, maxRedirects=12)`：跟随重定向链（b23.tv 短链、登录确认 URL 等），返回最终状态与 URL。

### 5.5 WBI 签名（`getWbiKeys` / `signedParams`）

B 站部分接口（如 `/x/player/wbi/v2` 字幕列表）需要 WBI 签名：

1. 从 `https://api.bilibili.com/x/web-interface/nav` 拉取 `wbi_img` 的 imgKey/subKey（带 12 小时缓存，失败用硬编码 fallback key）。
2. 用固定置换表 `MIXIN_KEY_ENC_TAB` 从 `imgKey+subKey` 生成 32 位 mixinKey。
3. `signedParams`：参数加 `wts` 时间戳 → 过滤特殊字符 → 按 key 排序拼接 → `md5(query + mixinKey)` 得到 `w_rid`。

### 5.6 DeepSeek 封装（`deepseekChat`）

统一入口：POST `https://api.deepseek.com/chat/completions`，模型默认 `deepseek-chat`，支持 `maxTokens`、`temperature`、超时（默认 180s）、父级 AbortController 联动。返回 `choices[0].message.content`。

### 5.7 议题成文引擎（核心亮点，见 §7.3）

免费搜索源四路并发：维基百科（zh.wikipedia.org API + extract）、DuckDuckGo（HTML 解析）、Bing（HTML 解析 `li.b_algo`）、SearXNG（5 个公共实例，JSON 优先、HTML 兜底）。结果按 hostname+pathname 去重、30 分钟缓存。

### 5.8 HTTP 服务与路由

```js
const server = http.createServer(async (req, res) => { ... });
server.listen(PORT, BIND_HOST, ...);
```

请求分发：
1. `url.pathname` 以 `/api/` 开头 → 走 API 路由（先过访问口令校验，POST 读 body）。
2. 否则 → `serveStatic` 提供 `public/` 下的静态文件（做了路径穿越防护：`filePath.startsWith(PUBLIC_DIR)` 校验）。

**API 路由完整清单：**

| 方法 | 路径 | 功能 | 关键逻辑 |
|---|---|---|---|
| GET | `/api/health` | 健康检查 | 返回端口、是否已有 Cookie、服务端是否配了 DeepSeek Key、是否需要访问口令 |
| POST | `/api/session/cookies` | 同步浏览器 Cookie 到服务端 jar | `setCookiesArray(body.cookies)` |
| GET | `/api/bili/nav` | 获取 B 站登录状态 | 代理 `/x/web-interface/nav`，返回 json + cookies |
| GET | `/api/bili/qrcode/generate` | 获取登录二维码 | 代理 passport qrcode/generate |
| GET | `/api/bili/qrcode/poll` | 轮询扫码结果 | 代理 poll；登录成功(code=0)后 `followWithCookies` 跟随确认 URL 收完整 Cookie |
| GET | `/api/bili/resolve` | 解析链接提取 BV 号 | 正则提取 `BV[0-9A-Za-z]+`；无 BV 则跟随重定向（b23.tv）再提取；同时解析分P参数 `p` |
| GET | `/api/bili/view` | 获取视频信息 | 代理 `/x/web-interface/view`（标题、cid、分P列表） |
| GET | `/api/bili/subtitles` | 获取字幕列表 | 用 WBI 签名请求 `/x/player/wbi/v2` |
| GET | `/api/bili/subtitle` | 下载字幕 JSON 内容 | 代理字幕 URL（自动补 `https:` 前缀） |
| POST | `/api/article/generate` | 议题成文（SSE 流） | 见 §5.9 |
| POST | `/api/article/cancel` | 取消议题成文 | abort 对应 requestId 的 controller |
| POST | `/api/ai/chat` | DeepSeek 通用对话（润色/科普/精简名用） | 直接代理，10 分钟超时，支持取消 |
| POST | `/api/ai/cancel` | 取消 AI 对话 | abort 对应 requestId 的 controller |

### 5.9 议题成文 SSE 接口细节

`POST /api/article/generate` 返回 `text/event-stream`，事件格式 `data: {json}\n\n`：

- 入参校验：DeepSeek API Key（body.apiKey 或服务端配置）、议题非空、≤200 字。
- 30 分钟总超时 + 20 秒心跳 `: ping` 防断连。
- 客户端断开（`req.on("close")`）自动 abort 整条生成链。
- 事件类型：`stage`（阶段提示）→ `outline`（大纲）→ `progress`（搜索/写作/导语/审校/修订进度）→ `done`（最终文章）| `error` | `cancelled`。
- 取消通过 `requestId` 查 `activeAIRequests` 表 abort。

---

## 6. 前端架构与模块详解

### 6.1 index.html 结构

单页应用，5 个 `<section class="view">` 由顶部 tab 切换：

1. **生成文章**（`#view-article`）：议题输入、补充要求、篇幅（3000/6000/10000）、风格（通俗/专业）、进度卡片（progress + status + log）、结果卡片（预览 + 立即阅读）。
2. **下载**（`#view-download`）：链接 textarea（每行一个）、格式复选框（TXT/SRT/ASS/VTT/JSON/LRC）、进度条、日志。
3. **文档**（`#view-docs`）：搜索框、导入按钮、文件列表。
4. **阅读**（`#view-reader`）：工具栏（返回、文件名、字数统计、夜间、A−/A+、导出）+ 内容区。
5. **设置**（`#view-settings`）：AI 设置（API Key、模型、润色分块字数、AI 精简文件名开关）、访问口令、B 站 Cookie 高级管理、关于。

两个弹窗：扫码登录（`#qr-modal`，canvas 渲染二维码）、AI 处理（`#ai-modal`，进度 + 日志 + 取消/完成）。

### 6.2 storage.js —— 数据层

**IndexedDB**（`biliSubWeb` 库，v1，`files` 对象仓库，keyPath `id`，索引 `updatedAt`）：

| 方法 | 功能 |
|---|---|
| `listFiles()` | 全部文件按 updatedAt 倒序 |
| `getFile(id)` | 取单个文件 |
| `saveFile(record)` | 保存/更新（自动生成 id、统计 size、更新 updatedAt） |
| `renameFile(id, name)` | 重命名并同步 ext |
| `deleteFile(id)` | 删除并清理该文件的 localStorage 阅读设置 |

文件记录结构：`{id, name, ext, content, size, createdAt, updatedAt, bvid, lang, meta}`。

**localStorage 键**：`biliSub_cookies`（B 站 Cookie）、`biliSub_ai`（AI 设置）、`biliSub_access_token`（访问口令）、`biliSub_theme`、`biliSub_font`（字号）、`biliSub_pos_<id>`（阅读位置比例）。

### 6.3 api.js —— 通信层

- `handle(resp)`：统一解析 JSON；**401 抛访问口令错误**；响应里带 `cookies` 数组时自动回存 localStorage（这是登录 Cookie 闭环的关键）。
- `authHeaders()`：localStorage 里的访问口令以 `X-Access-Token` 头发送。
- `articleGenerate(payload, onEvent, signal)`：SSE 专用——用 `fetch` 读流，按 `\n\n` 切分 `data:` 行，JSON.parse 后回调 `onEvent`。

### 6.4 ai.js —— AI 功能层

**提示词组装**（`buildPolishSystem` / `buildArticleSystem`）：前端 `fetch("skills/...")` 读取 md 文件拼进 system prompt。这是"技能文件"机制——提示词作为静态资源存放，改提示词无需改代码。

**语言检测**（`detectLanguage` / `languageRequirement`）：CJK 字符占比 > 15% 判为中文；非中文时在 system prompt 追加"最终输出必须全部使用简体中文"的要求。

**字幕润色**（`runPolish`）：
1. 按行分块（默认 4500 字/块，可配置）。
2. 逐块调用 DeepSeek（每块 system=技能全文，user=块内容；非首块附带上一块输出结尾 800 字保持风格衔接）。
3. 全部块完成后，基于开头 6000 字生成"导读"。
4. 拼装输出：`## 视频信息` + `## 导读` + `## 正文`，保存为 `{原名}-润色版.md`。

**科普文章**（`runArticle`）：
- ≤30000 字字幕：一次请求生成整篇（标题+导读+正文+核心记忆点+信源）。
- >30000 字：分 2~6 段连续生成；每段都带**完整字幕 + 前文结尾 2500 字**，明确要求"只写还没写过的内容、宁多勿漏、绝不重复"，从根上避免长文重复/遗漏。

**AI 精简文件名**（`shortFileName`）：把视频标题精简为 ≤7 个汉字的中文文件名（0 温度、maxTokens=30），失败时 `fallbackShortName` 纯正则截断兜底。

### 6.5 视图模块（v7 起从 app.js 拆分）

v7 起，原 1154 行的 `app.js` 拆分为「核心 + 6 个视图模块 + 入口」，全部挂在 `window.ui` 命名空间下：

| 模块 | 职责 | 关键函数 |
|---|---|---|
| `core.js` | 状态机 + 工具函数 + 视图切换 + 主题/字号 + AI 设置 | `state`、`$`、`switchView`、`applyTheme/applyFont`、`loadAiSettings/saveAiSettings/aiSettings`、`appendLog/setDlProgress`、`sleep/escapeHtml/sanitize/formatBytes/debounce/showToast` |
| `login.js` | 扫码登录 + 手动 Cookie | `refreshLoginStatus`、`startQr`、`pollQr`、`stopQrPolling`、`saveCookiesManually`、`clearCookies` |
| `download.js` | 字幕下载 + 格式转换 | `startDownload`、`selectPreferred`、`parseSubtitleBody`、`cleanSegments`、`generateFormat` |
| `docs.js` | 文档管理 | `refreshDocs`、`importFiles`、`exportFile`、`renameFile`、`deleteFile` |
| `reader.js` | 阅读器 | `openReader`、`saveReaderPos` |
| `ai-modal.js` | 润色/科普弹窗 | `startAi`、`cancelAi`、`openAiModal`、`appendAiLog` |
| `article.js` | 议题成文 UI | `startArticle`、`cancelArticle`、`handleArticleEvent`、`setArticleProgress` |
| `app.js` | 入口 | `init()`（初始化流程）、`bindEvents()`（全部事件绑定） |

- **状态机** `ui.state`（core.js）：当前视图、文件列表、当前文件、下载运行/取消标志、二维码轮询、AI/议题成文的 busy/abort/requestId 等。
- **初始化** `init()`（app.js）：应用主题/字号 → 加载 AI 设置 → 绑定事件 → 同步本地 Cookie 到服务端 → 刷新登录状态 → 刷新文档 → 健康检查（若服务端配了 Key，隐藏"请填 Key"提示；若需要口令且本地没有，弹 prompt 输入）。
- **视图切换** `switchView()`（core.js）：切换 tab 高亮与 section 显隐；进"文档"页刷新列表，进"阅读"页无文件时显示空状态。
- **下载流程** `startDownload()`（download.js）：见 §7.2。
- **文档管理**（docs.js）：`refreshDocs()` 渲染列表（图标、名称、大小/时间/语言、展开操作区）；`exportFile()` 优先 Web Share API 分享文件，否则下载；`importFiles()` 用 FileReader 读 ArrayBuffer，先按 UTF-8 解码，若出现 >2 个替换字符 `\uFFFD` 则改用 GB18030 重解（兼容中文编码字幕文件）。
- **阅读器** `openReader()`（reader.js）：md 用 marked 渲染（`breaks:true, gfm:true`），其他格式 `<pre>` 转义显示；记住滚动比例；滚动防抖保存位置。
- **议题成文** `startArticle()` + `handleArticleEvent()`（article.js）：见 §7.3；进度百分比映射：搜索 8~40%、写作 40~72%、导语 72~78%、审校 78~86%、修订 86~96%；`done` 事件自动保存并打开阅读器。
- **AI 弹窗** `startAi()`（ai-modal.js）：润色/科普共用，`ui.state.aiRequestId` 用于服务端取消。

> 跨模块调用统一走 `ui.xxx`（运行时解析，不依赖定义顺序），因此视图模块加载顺序只要保证 `core.js` 最先即可。

---

## 7. 核心工作流拆解

### 7.1 扫码登录工作流

```
[前端] 点「扫码登录」
  └─ api.qrGenerate()  →  POST/GET /api/bili/qrcode/generate
        └─ [后端] 代理 B 站 passport qrcode/generate → 返回 url + qrcode_key + 新 cookies
  └─ QRCode.toCanvas(url) 渲染二维码
  └─ setInterval 每 2s 调 api.qrPoll(key)  →  /api/bili/qrcode/poll
        └─ [后端] 代理 B 站 poll：
             code=86101 已扫码待确认 / 86090 请点确认 / 86038,-2 过期
             code=0 登录成功 → followWithCookies(确认URL) 跟随重定向，把完整登录 Cookie 收进 jar
  └─ [前端] 每次响应自动把 data.cookies 存 localStorage（api.js handle()）
  └─ 刷新登录状态（/api/bili/nav 的 data.isLogin）
```

**闭环要点**：Cookie 的"家"是浏览器 localStorage；每次页面加载 `init()` 时 `sendCookies` 全量同步给服务端内存 jar；服务端代理任何 B 站请求都会把新 Set-Cookie 回传，前端自动存回。

### 7.2 字幕下载工作流（每行链接）

```
api.resolve(url)
  ├─ 正则提取 BV 号；没有则 followWithCookies 跟随重定向（处理 b23.tv）再提取
  └─ 返回 {bvid, page(分P), url}
api.view(bvid)          → 视频信息（title、pages[]、每 P 的 cid）
api.subtitles(bvid,cid) → WBI 签名请求，拿字幕列表 subtitle.subtitles[]
selectPreferred(list)   → 语言排序：zh(0) < en(1) < 其他(2)，取第一个
api.subtitle(subUrl)    → 下载字幕 JSON（body[]: {from,to,content}）
parseSubtitleBody       → 转 [{start, end, content, lang}]
cleanSegments           → 清理：去"关注...获取更多精彩内容"、去#话题#、去连续破折号、压缩空白；
                          相邻两段间隔<0.5s 且都<20字时合并（断句拼接）
可选 AI 精简文件名      → ai.shortFileName(title) 失败则 fallbackShortName
generateFormat(segments, fmt, info)
  ├─ txt：每行一条内容
  ├─ srt：序号 + HH:MM:SS,mmm --> HH:MM:SS,mmm
  ├─ vtt：WEBVTT 头 + 点分隔毫秒
  ├─ json：{title,bvid,lang,platform,subtitles[]}
  ├─ lrc：[ti:][length:] + [MM:SS.xx]内容
  └─ ass：Script Info + V4+ Styles（中英双行分 ZH/EN 样式，\n 分隔时拆两行 Dialogue）
storage.saveFile → IndexedDB
```

### 7.3 议题成文工作流（融合 STORM + GPT Researcher）

```
POST /api/article/generate（SSE）
│
├─ ① 规划 (plan)
│    deepseekChat 输出 JSON：{title, summary, perspectives[2-3], sections[{title, questions[2], keywords[3-5]}]}
│    extractOutline() 解析：先找 {} 提 JSON，失败则按行兜底（章节行正则）
│    兜底：无有效章节时套用固定 5 章模板（背景/原理/案例/争议/趋势）
│
├─ ② 搜索 (search，每章并发 2 worker)
│    queries = 章节研究问题 + 关键词 + 章节标题，取前 2 个
│    searchQuery 四路并发：维基百科 + DuckDuckGo + Bing + SearXNG
│    合并去重 → 取前 6 条 → 前 3 条且非 youtube/bilibili/PDF/维基 的抓正文
│    fetchText 抓页面 → htmlToArticleText 提取标题+段落纯文本（6000 字截断）→ 每源 notes{title,url,body}
│
├─ ③ 写作 (write，串行)
│    每章调用 writeSection：system 为"只依据编号资料写作、不编造、句末[n]标注"
│    user 含：议题/标题/视角/全文章节/本章研究问题/编号资料/上一章结尾 500 字（衔接语气）
│    每章目标字数 = 总字数/章节数，maxTokens 按目标字数×1.8 计算
│
├─ ④ 导语 (lead)
│    writeLeadSection：基于草稿前 12000 字 + 前 25 个来源编号，写"## 导语"（4 段 400-600 字）
│
├─ ⑤ 审校 (review)
│    reviewArticle：审查 编造/重复/问题回答/引用规范/篇幅，输出 JSON {ok, issues[{section,problem,suggestion}]}
│
├─ ⑥ 修订 (revise，最多 2 章)
│    reviseSection：按审校意见重写问题章节，保留编号引用
│
└─ ⑦ 组装 (done)
     `# 标题` + `> 摘要` + 导语 + 各章 + `## 参考资料`（编号列表）+ 生成说明脚注（含实际字数）
     通过 SSE 发 done 事件 → 前端保存为 {精简标题}-文章.md → 自动打开阅读器
```

**进度映射（前端）**：搜索 8~40% → 写作 40~72% → 导语 72~78% → 审校 78~86% → 修订 86~96% → 完成 100%。

### 7.4 字幕润色工作流

```
[文档] 选文件 → 润色
  buildPolishSystem()：SKILL.md + common-errors.md 拼 system
  languageRequirement()：非中文强制简体中文输出
  splitTextForPolish(content, 4500)：按行分块
  逐块 chat()：system=技能全文+语言要求；首块"精修第1/N块"，
              后续块附上一块结尾800字"保持风格一致、不重复"
  全部块后：chat() 基于正文前6000字写"导读"（stripDigestPrefix 去前缀）
  输出 {name: 原名-润色版.md, content: 视频信息+导读+正文}
  保存到 IndexedDB，meta.from 记录源文件名
```

### 7.5 科普文章工作流（runArticle）

```
buildArticleSystem()：SKILL.md + rhetoric-discipline.md + writing-guidelines.md 拼 system
≤30000 字：一次生成（标题+导读+正文+核心记忆点+信源），maxTokens 8000
>30000 字：分 ceil(len/30000) 段（2~6 段）
  第1段：标题+导读+开头
  中间段/末段：完整字幕 + 前文结尾2500字，要求"继续写尚未覆盖内容、绝不重复、宁多勿漏"
  末段追加 endingRules（核心记忆点 + 信源）
输出 {name: 原名-大众科普版.md}
```

---

## 8. 数据存储方案

| 数据 | 存储位置 | 键/结构 | 生命周期 |
|---|---|---|---|
| 文档文件（字幕/文章/导入） | IndexedDB `biliSubWeb` / `files` | `{id,name,ext,content,size,createdAt,updatedAt,bvid,lang,meta}` | 永久，清浏览器数据才丢 |
| B 站 Cookie | localStorage `biliSub_cookies` | 数组 `[{name,value,domain,path,expires,secure}]` | 永久（B 站侧会过期） |
| AI 设置 | localStorage `biliSub_ai` | `{apiKey,model,chunkSize,shortName}` | 永久 |
| 访问口令 | localStorage `biliSub_access_token` | 字符串 | 永久 |
| 主题/字号 | localStorage `biliSub_theme` / `biliSub_font` | 字符串 | 永久 |
| 阅读位置 | localStorage `biliSub_pos_<id>` | 滚动比例 0~1 | 删文件时清除 |
| B 站 Cookie（服务端） | server.js 内存 `jar` | Map | 服务重启丢失，下次前端同步恢复 |

> 设计取向：**服务端无状态**（除内存 jar 和缓存），可自由扩容/重启；数据跟着浏览器走，天然多端隔离。

---

## 9. 安全与配置机制

### 9.1 访问口令（ACCESS_TOKEN）

- 配置来源：环境变量 `ACCESS_TOKEN` 或 `server-config.json`。
- 校验规则（server.js 入口）：除 `/api/health` 外**所有** `/api/` 请求都必须带 `X-Access-Token` 头且值匹配，否则 401。
- 前端：设置页输入 → 存 localStorage → `authHeaders()` 自动附加。未配置口令时全站开放。
- 作用：云端部署后防止陌生人蹭用 DeepSeek Key 额度。

### 9.2 DeepSeek Key 分层

- 服务端配置（环境变量 `DEEPSEEK_API_KEY`）：云端部署时用，前端可不填。
- 前端填写：存在 localStorage，随请求 body 发给本地服务，仅用于代理调用。
- 优先级：请求体 apiKey > 服务端配置。

### 9.3 路径安全

- 静态文件：`path.normalize` 后校验 `filePath.startsWith(PUBLIC_DIR)`，防目录穿越。
- 请求体：`readBody` 限制 30MB。

### 9.4 隐私说明

- B 站 Cookie、DeepSeek Key 都只存本机浏览器；服务端不落盘（除非手动写 server-config.json）。
- `.gitignore` 明确排除 `server-config.json`、日志、云隧道工具，防止密钥上传 GitHub。

---

## 10. 部署与发布流程

### 10.1 本地运行（开发测试）

```powershell
cd D:\deepseek_h_pro\biliSub-cloud-package
node server.js
# 浏览器打开 http://127.0.0.1:8324
```

需要 Node.js ≥ 20（用了全局 fetch、AbortController、getSetCookie）。

### 10.2 云部署（Render）

`render.yaml` 定义：

```yaml
services:
  - type: web
    name: bilisub
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: node server.js
    envVars:
      - key: DEEPSEEK_API_KEY    # 手动填
      - key: ACCESS_TOKEN        # 手动填
      - key: BIND_HOST
        value: 0.0.0.0           # 必须 0.0.0.0 才能对外访问
```

流程：GitHub 建仓库 → 上传源码（不含 node_modules/server-config.json）→ Render 选 Blueprint 识别 render.yaml → 填两个密钥 → Apply 部署 → 访问 `https://bilisub.onrender.com`。

更新流程：改代码 → 运行 `.\sync-to-git.ps1 -Message "说明" -Push`（自动同步 + commit + push）→ Render 自动重新部署（1~3 分钟）。手动部署入口：Render 控制台 → Service（ID `srv-d9qa5sajobas73bngjjg`）→ Manual Deploy。

### 10.3 免费实例注意事项

- 闲置 15 分钟休眠，唤醒 30~60 秒；可用 UptimeRobot 每 5 分钟探活保持常驻。
- B 站接口从海外服务器调用，个别视频可能拿不到字幕，可稍后重试或用本地版。

---

## 11. 关键实现细节与注意事项

1. **零依赖可运行**：server.js 只用内置模块 + 全局 fetch。若某天要离线/瘦身部署，`npm install` 甚至可以省略（但 lock 文件已提交，保持常规流程更稳）。
2. **WBI 签名会过期**：`FALLBACK_WBI_KEYS` 是写死的备用 key，若 B 站更换算法需同步更新置换表与 fallback key。
3. **SSE 心跳**：20 秒 `: ping` 注释行防代理/浏览器断流；30 分钟总超时兜底。
4. **取消链路**：前端 AbortController.abort() + 后端 requestId 查表 abort，两端同时断，生成链内的 fetch 都挂在该 signal 上（`fetchText`/`deepseekChat` 都做了 signal 联动）。
5. **并发控制**：议题搜索用 `runPool(items, 2, fn)` 限 2 并发，避免打爆免费搜索源；写作串行保证衔接。
6. **编码兼容**：导入文件 UTF-8 解码出现大量 `\uFFFD` 时自动切 GB18030 重解。
7. **语言自动选择**：`langRank` 把 `ai-zh` 前缀剥掉再判 zh；zh < en < 其他，只导出最优一条。
8. **字幕清洗规则**：删除 B 站水印"关注…获取更多精彩内容"、`#话题#`、连续破折号；短句（<20字）且间隔<0.5s 自动拼接。
9. **ASS 双语样式**：字幕内容含 `\n`（AI 字幕常为 中英双行）时拆成 ZH/EN 两条 Dialogue，各用独立样式。
10. **标记/工具函数**：`marked` 只在浏览器端用于 md 渲染；`qrcode` 只在扫码弹窗用；服务端不引入。
11. **`mixinKey` 生成**：`MIXIN_KEY_ENC_TAB` 是 64 个索引的置换表，把 `imgKey+subKey` 重排后取前 32 位——B 站 WBI 标准的实现细节，改 B 站算法时必须同步。
12. **`truncate` 用省略号** `…` 而非 `...`：所有喂给模型的内容都做了长度截断，控制 token 成本。
13. **安全边界**：`/api/bili/subtitle` 允许任意 https URL 代理下载（前端传入 B 站字幕地址），存在一定 SSRF 面，仅限自用场景。
14. **history 残留**：node_modules 里的 youtube-transcript/jsdom 等是历史遗留，勿手动 `require`；`npm ci` 不会装它们。

---

## 12. 常见问题排查

| 现象 | 排查思路 |
|---|---|
| 页面提示"无法连接本地服务" | 是否在 biliSub-cloud-package 目录运行了 `node server.js`；端口是否被占用（改 `PORT` 环境变量） |
| 下载提示未登录/无字幕 | 先扫码登录；B 站部分视频字幕需要登录或会员；海外服务器偶发失败可重试 |
| 云部署打开很慢/打不开 | 免费实例休眠唤醒需 30~60 秒；看 Render Logs：`Build successful` → `Running 'node server.js'` |
| 访问口令错误 401 | 前端设置页重新填写 ACCESS_TOKEN；确认环境变量已配置 |
| AI 报"缺少 DeepSeek API Key" | 本地版在设置页填 Key；云端版确认 `DEEPSEEK_API_KEY` 已配且 `/api/health` 的 `hasAiKey` 为 true |
| 议题成文中断 | 检查网络/SSE 心跳；30 分钟超时自动取消；单章写作 240s 超时 |
| 部署后接口 404 | 确认 public 目录完整上传（网页上传易漏目录结构） |
| 语法错误部署失败 | 本地先 `node --check server.js` |

---

## 附：源码速查索引

| 想找什么 | 去哪里 |
|---|---|
| 入口与静态文件服务 | `server.js` |
| 所有 API 路由 | `lib/routes.js` `handleApi()` |
| B 站 Cookie 罐 | `lib/cookie-jar.js` |
| B 站请求 + WBI 签名 | `lib/bili-client.js` |
| DeepSeek 对话 | `lib/deepseek.js` |
| 议题成文引擎 | `lib/article-engine.js`（plan/search/write/lead/review/revise） |
| 免费搜索源 | `lib/search.js`（wiki/DuckDuckGo/Bing/SearXNG） |
| 配置加载 | `lib/config.js` |
| 通用工具 | `lib/utils.js` |
| 下载主流程 | `public/js/download.js` `startDownload()` |
| 字幕格式生成 | `public/js/download.js` `generateFormat()` |
| 文档管理 | `public/js/docs.js` |
| 阅读器 | `public/js/reader.js` |
| 扫码登录 | `public/js/login.js` |
| 润色/科普弹窗 | `public/js/ai-modal.js` |
| 议题成文 UI | `public/js/article.js` |
| 状态机 + 工具 | `public/js/core.js` |
| 入口 + 事件绑定 | `public/js/app.js` |
| 润色/科普提示词组装 | `public/js/ai.js` `buildPolishSystem/buildArticleSystem` |
| 技能文件 | `public/skills/transcript-polisher/`、`public/skills/science-writing/` |
| 前端数据层 | `public/js/storage.js`（IndexedDB + localStorage） |
| 通信层 | `public/js/api.js`（含 SSE 解析） |
| 同步脚本 | `sync-to-git.ps1` |
| 部署配置 | `render.yaml`、`发布部署指南.md`、`云部署说明.md` |
