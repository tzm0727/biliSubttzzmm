# biliSub 免费云部署说明（Render）

部署完成后会得到一个**永久固定**的手机链接，例如：
`https://bilisub.onrender.com`

以后手机直接用这个链接，电脑可以关机。

## 需要准备（都是免费的）

1. GitHub 账号（免费）：https://github.com
2. Render 账号（免费，不要信用卡）：https://render.com

## 第一步：把代码传到 GitHub

1. 登录 GitHub，点右上角 `+` → `New repository`
2. 仓库名随意，例如 `bilisub`，选 **Public**，不要勾选任何初始化选项，点 `Create repository`
3. 进入仓库后，点 `uploading an existing file`
4. 把本文件夹里的**所有文件**拖进去（public 文件夹、server.js、package.json、render.yaml 等），点 `Commit changes`

> 注意：本压缩包里**没有** server-config.json，也没有 node_modules，Key 不会上传。

## 第二步：在 Render 部署

1. 打开 https://render.com ，用 GitHub 账号注册/登录
2. 点 `New +` → `Blueprint`
3. 选择你刚创建的 `bilisub` 仓库
4. Render 会自动识别，然后让你填两个秘密值：
   - `DEEPSEEK_API_KEY`：填你的 DeepSeek Key（例如 sk-89bc...）
   - `ACCESS_TOKEN`：自己设一个访问口令（例如 bilisub2026），手机打开时要用
5. 点 `Apply`，等 3~5 分钟构建完成
6. 完成后打开 `https://bilisub.onrender.com`（名字可在 Blueprint 里改），看到页面就成功了

## 手机使用

- 打开固定链接，第一次可能要等 30~60 秒（免费实例休眠唤醒，属正常）
- 输入访问口令（就是你设置的 ACCESS_TOKEN）
- 以后每次打开同一个链接，不用再换

## 说明

- Render 免费实例闲置 15 分钟会休眠，但链接不变；再访问时自动唤醒
- B 站接口从国外服务器调用，绝大多数视频正常；个别视频若提示无字幕，可稍后再试或继续用本地隧道版
- YouTube 字幕也支持（无需登录）；已改用安卓客户端接口（youtube-transcript-plus）+ 自动后备方案，并带 30 分钟缓存，能明显减少数据中心 IP 限流；若仍提示限流，稍后再试即可
- 想在电脑上继续用本地版也可以，两者互不影响
